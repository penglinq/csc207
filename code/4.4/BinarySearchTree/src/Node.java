/*This separated class declaration can be used when class Node
 * is not defined in class BinarySearchTree.
 * However, this class is not used in this project now.
 * 
 */
/*public class Node<T extends Comparable<T>> {
		public T value;
		public Node<T> left, right;
		
		public Node(T v) {
			value = v;
		}
		
		public Node(T v, Node<T> l, Node<T> r) {
			value = v;
			left = l;
			right = r;
		}
	}*/